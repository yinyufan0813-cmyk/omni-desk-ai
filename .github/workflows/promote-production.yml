name: Promote Production

on:
  workflow_dispatch:
    inputs:
      artifact_run_id:
        description: Release workflow run id containing dist-and-sbom artifact
        required: true
      artifact_name:
        description: GitHub Actions artifact name
        required: true
        default: dist-and-sbom
      expected_version:
        description: Expected package version
        required: true
        default: 1.12.7+root-monorepo-production-ga-candidate
      deploy_mode:
        description: "Fixed deploy adapter mode: docker-compose, kubectl, or systemd. noop is forbidden for production."
        required: true
        default: docker-compose

permissions:
  contents: read
  actions: read
  attestations: read
  id-token: write

concurrency:
  group: omnidesk-production-promotion
  cancel-in-progress: false

env:
  EXPECTED_VERSION: ${{ inputs.expected_version }}

jobs:
  promotion-gate:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.11"
      - name: Forbid no-op production deploys
        run: |
          if [ "${{ inputs.deploy_mode }}" = "noop" ]; then
            echo "noop deploy mode is forbidden for production promotion" >&2
            exit 2
          fi
      - run: python -m pip install --upgrade pip
      - run: python -m pip install --require-hashes -r requirements.dev.lock
      - run: python -m pip install -e . --no-deps --no-build-isolation
      - run: |
          python scripts/check_lock_hashes.py requirements.runtime.lock
          python scripts/check_lock_hashes.py requirements.bootstrap.lock
          python scripts/check_lock_hashes.py requirements.dev.lock
          python scripts/check_lock_hashes.py requirements.security.lock
      - run: PYTHONPATH=. python -m pytest -q -W error --timeout=60 --timeout-method=thread
      - name: Download signed release artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p dist
          gh run download "${{ inputs.artifact_run_id }}" --name "${{ inputs.artifact_name }}" --dir dist
      - name: Verify immutable release artifact
        env:
          OMNIDESK_RELEASE_SIGNING_KEY: ${{ secrets.OMNIDESK_RELEASE_SIGNING_KEY }}
        run: |
          python scripts/verify_release_artifact.py dist --expected-version "$EXPECTED_VERSION" --require-signatures --require-metadata
          python scripts/verify_release_signatures.py dist
          wheel="$(find dist -maxdepth 1 -type f -name '*.whl' -print -quit)"
          test -n "$wheel"
          {
            echo "OMNIDESK_ARTIFACT_SHA256=$(sha256sum "$wheel" | awk '{print $1}')"
            echo "OMNIDESK_IMAGE_DIGEST=$(python scripts/read_release_metadata.py dist image.digest)"
            echo "OMNIDESK_IMAGE_REF=$(python scripts/read_release_metadata.py dist image.ref)"
          } >> "$GITHUB_ENV"

      - name: Install Cosign
        run: go install github.com/sigstore/cosign/v2/cmd/cosign@v2.4.1
      - name: Verify release container image signature and attestations
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          test -n "$OMNIDESK_IMAGE_REF"
          test -n "$OMNIDESK_IMAGE_DIGEST"
          cosign verify "$OMNIDESK_IMAGE_REF@$OMNIDESK_IMAGE_DIGEST" --certificate-identity-regexp "https://github.com/${GITHUB_REPOSITORY}/" --certificate-oidc-issuer "https://token.actions.githubusercontent.com"
          cosign verify-attestation "$OMNIDESK_IMAGE_REF@$OMNIDESK_IMAGE_DIGEST" --type spdx --certificate-identity-regexp "https://github.com/${GITHUB_REPOSITORY}/" --certificate-oidc-issuer "https://token.actions.githubusercontent.com"
          cosign verify-attestation "$OMNIDESK_IMAGE_REF@$OMNIDESK_IMAGE_DIGEST" --type slsaprovenance --certificate-identity-regexp "https://github.com/${GITHUB_REPOSITORY}/" --certificate-oidc-issuer "https://token.actions.githubusercontent.com"

      - name: Verify Cosign signatures and attestations
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          wheel="$(find dist -maxdepth 1 -type f -name '*.whl' -print -quit)"
          test -n "$wheel"
          python scripts/list_cosign_artifacts.py dist --null | while IFS= read -r -d '' artifact; do
            test -f "$artifact"
            cosign verify-blob "$artifact" --certificate "$artifact.cosign.pem" --signature "$artifact.cosign.sig" --certificate-identity-regexp "https://github.com/${GITHUB_REPOSITORY}/" --certificate-oidc-issuer "https://token.actions.githubusercontent.com"
          done
          cosign verify-attestation "$wheel" --type slsaprovenance --certificate dist/slsa.intoto.pem --signature dist/slsa.intoto.sig --certificate-identity-regexp "https://github.com/${GITHUB_REPOSITORY}/" --certificate-oidc-issuer "https://token.actions.githubusercontent.com"

      - name: Verify artifact attestation
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          test -f dist/release_metadata.json
          test -f dist/release_signatures.json
          find dist -maxdepth 1 -type f -print0 | sort -z | while IFS= read -r -d '' artifact; do
            gh attestation verify "$artifact" --repo "$GITHUB_REPOSITORY"
          done
      - name: Require complete Real GA evidence before production promotion
        run: python scripts/check_real_ga_complete.py . --evidence-dir dist/external-evidence
      - name: Deploy signed artifact to production
        env:
          OMNIDESK_DEPLOY_MODE: ${{ inputs.deploy_mode }}
          OMNIDESK_EXPECTED_VERSION: ${{ inputs.expected_version }}
          OMNIDESK_ARTIFACT_SHA256: ${{ env.OMNIDESK_ARTIFACT_SHA256 }}
          OMNIDESK_IMAGE_DIGEST: ${{ env.OMNIDESK_IMAGE_DIGEST }}
          OMNIDESK_DEPLOY_COMPOSE_FILE: ${{ vars.OMNIDESK_PRODUCTION_COMPOSE_FILE }}
          OMNIDESK_DEPLOY_SERVICE: ${{ vars.OMNIDESK_PRODUCTION_SERVICE }}
          OMNIDESK_DEPLOY_KUBE_CONTEXT: ${{ vars.OMNIDESK_PRODUCTION_KUBE_CONTEXT }}
          OMNIDESK_DEPLOY_NAMESPACE: ${{ vars.OMNIDESK_PRODUCTION_NAMESPACE }}
          OMNIDESK_DEPLOYMENT_NAME: ${{ vars.OMNIDESK_PRODUCTION_DEPLOYMENT_NAME }}
          OMNIDESK_CONTAINER_NAME: ${{ vars.OMNIDESK_PRODUCTION_CONTAINER_NAME }}
          OMNIDESK_IMAGE: ${{ vars.OMNIDESK_PRODUCTION_IMAGE }}
          OMNIDESK_DEPLOY_HOST: ${{ vars.OMNIDESK_PRODUCTION_HOST }}
          OMNIDESK_DEPLOY_USER: ${{ vars.OMNIDESK_PRODUCTION_USER }}
          OMNIDESK_REMOTE_DEPLOY_SCRIPT: ${{ vars.OMNIDESK_PRODUCTION_REMOTE_DEPLOY_SCRIPT }}
        run: |
          artifact="$(find dist -maxdepth 1 -type f -name '*.whl' -print -quit)"
          test -n "$artifact"
          bash scripts/deploy_artifact.sh --artifact "$artifact" --environment production --mode "$OMNIDESK_DEPLOY_MODE" --expected-version "$EXPECTED_VERSION" --artifact-sha256 "$OMNIDESK_ARTIFACT_SHA256" --image-digest "$OMNIDESK_IMAGE_DIGEST"
      - name: Production smoke
        env:
          OMNIDESK_SMOKE_BASE_URL: ${{ vars.OMNIDESK_PRODUCTION_BASE_URL }}
          OMNIDESK_SMOKE_ADMIN_TOKEN: ${{ secrets.OMNIDESK_PRODUCTION_ADMIN_TOKEN }}
          OMNIDESK_SMOKE_SANDBOX_URL: ${{ vars.OMNIDESK_PRODUCTION_SANDBOX_URL }}
          OMNIDESK_SMOKE_SANDBOX_TOKEN: ${{ secrets.OMNIDESK_PRODUCTION_SANDBOX_TOKEN }}
          OMNIDESK_SMOKE_SANDBOX_HMAC_SECRET: ${{ secrets.OMNIDESK_PRODUCTION_SANDBOX_HMAC_SECRET }}
        run: python scripts/production_smoke_test.py --check-admin-metrics --check-admin-slo --fail-on-slo --strict-sandbox --expected-version "$EXPECTED_VERSION" --expected-artifact-sha256 "$OMNIDESK_ARTIFACT_SHA256"
      - name: Production SLO gate
        if: ${{ vars.OMNIDESK_PRODUCTION_SLO_METRICS_FILE != '' }}
        run: python scripts/check_slo.py --metrics-file "${{ vars.OMNIDESK_PRODUCTION_SLO_METRICS_FILE }}" --fail-on-error-budget
      - run: PYTHONPATH=. python -m pytest tests/test_shadow_canary_upgrade_memory.py tests/test_upgrade_state_machine.py -q
