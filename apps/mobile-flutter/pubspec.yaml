name: omnidesk_mobile
version: 1.12.7+127
description: Omni mobile approval, chat, and notification app.
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^11.0.0
  local_auth: ^3.0.2
  firebase_core: ^4.12.1
  firebase_messaging: ^16.4.3
  cryptography: ^2.7.0
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
