# Security floors for packages with known advisories in dependency audits.
#
# Keep this file small and explicit: it is used by uv lock generation to
# prevent dependency resolution from drifting back onto vulnerable releases.
click>=8.4.2
cryptography>=48.0.1
filelock>=3.31.1
msgpack>=1.2.1
pillow>=12.3.0
pip>=26.1.2
pytest>=9.0.3
requests>=2.34.2
setuptools>=83.0.0
starlette>=1.0.1
urllib3>=2.7.0
wheel>=0.47.0
